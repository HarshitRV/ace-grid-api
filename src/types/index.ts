/**
 * Compatibility barrel for existing imports.
 *
 * New schema/type definitions live under `src/schemas`.
 * Keep this file as a non-breaking alias until all imports are migrated.
 */
export * from "@/schemas/index.js";
